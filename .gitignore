node_modules/
dist/
build/
.turbo/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo

# React Native / iOS / macOS (added when those apps are scaffolded)
ios/Pods/
ios/build/
macos/Pods/
macos/build/
*.xcuserstate
*.xcworkspace/xcuserdata/

# Vite
.vite/

# mcp-server: suzume.wasm copied in from node_modules before each dev/deploy
# (scripts/copy-suzume-wasm.mjs) so Wrangler's wasm module rule can see it -- regenerated,
# never hand-edited.
apps/mcp-server/src/vendor/
packages/mcp-server/src/vendor/

# Added by ggshield
.cache_ggshield
